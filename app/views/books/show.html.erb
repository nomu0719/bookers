 <body class='<%= controller.controller_name %>' >
 <main>
 <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<h4>Title:</h4>
<%= @book.title %>
<h4>body:</h4>
<%= @book.body %>

<%= link_to "Edit", edit_book_path(@book) %>
|
<%= link_to "Back", books_path %>

<%= yield %>
</main>
</body>